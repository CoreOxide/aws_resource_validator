{# Jinja2 template for aws_resource_validator/class_definitions.py. #}
# This file is auto-generated by aws_resource_validator.generator.pipeline_a.
# Do not edit manually; run `arv-generate pipeline-a` to refresh it.
from __future__ import annotations

from aws_resource_validator.core import APIObject, APIRegistry, Service

{% for service in services %}
class {{ service.name }}(Service):
    def __init__(self) -> None:
        super().__init__({{ service.name | repr }})
        {% for obj in service.api_objects -%}
        self.add_api_object(APIObject(
            name={{ obj.name | repr }},
            type={{ obj.type | repr }},
            pattern={{ obj.pattern_literal }},
            min_length={{ obj.min_length | repr }},
            max_length={{ obj.max_length | repr }},
        ))
        {% endfor %}

{% endfor %}
class_registry = APIRegistry()
{% for service in services %}
{{ service.instance_name }} = {{ service.name }}()
class_registry.add({{ service.instance_name }})
{% endfor %}
